name: Restore Running Mautic Workflow - Test
on:
  workflow_dispatch:
    inputs:
      brand:
        description: 'Brand identifier (optional, defaults to "default")'
        required: false
        default: 'default'
      restore_prefix:
        description: 'Prefix of the backup to restore (e.g., 2025-12-02)'
        required: true
jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key

      - name: Restore Mautic
        run: |
          set -euo pipefail

          BRAND="${{ github.event.inputs.brand }}"
          RESTORE_PREFIX="${{ github.event.inputs.restore_prefix }}"
          echo "♻ Restore requested for brand: $BRAND, prefix: $RESTORE_PREFIX"
          
          scp -o StrictHostKeyChecking=no -i ~/.ssh/vps_key scripts/restore_mautic.sh root@${{ vars.LINODE_IP }}:/tmp/restore_mautic.sh

          ssh -o StrictHostKeyChecking=no -i ~/.ssh/vps_key root@${{ vars.LINODE_IP }} \
          "MYSQL_PASSWORD='${{ secrets.MYSQL_PASSWORD }}' \
          MYSQL_ROOT_PASSWORD='${{ secrets.MYSQL_ROOT_PASSWORD }}' \
          bash /tmp/restore_mautic.sh '$BRAND' '$RESTORE_PREFIX'"

          echo "🟢 Restore completed successfully."
